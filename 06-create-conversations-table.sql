CREATE TABLE conversation (
    user_name VARCHAR(200),
    employer_name VARCHAR(150),
    message TEXT,
    date_sent TIMESTAMP
) 

 --we would've used just 'DATE' if we do not need the hours/minutes